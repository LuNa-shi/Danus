#!/usr/bin/env bash
# Launch the authenticated Web Console; configuration comes from env.sh.
set -euo pipefail
HERE="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
. "$HERE/../scripts/env.sh"
exec "$DANUS_PY" -m danus.web_console "$@"
