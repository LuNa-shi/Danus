# Domain docs

This repository uses a single domain context. Before designing or changing the Danus Web Console, read the root `CONTEXT.md` and relevant decisions in `docs/adr/`. Use the glossary terms exactly; surface conflicts with accepted ADRs instead of silently overriding them.
